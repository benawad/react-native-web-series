// pick-up the app defined in common, export it into this project target as "local app"
// if @wow/common shows "can't find", look at "main" setting in common/package.json
import { App } from "@wow/common";

export default App;